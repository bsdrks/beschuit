env:
    browser: true
    es2021: true
extends: [prettier]
parserOptions:
    ecmaVersion: latest
    sourceType: module
rules:
    {
        semi: ['error', 'always'],
        indent: ['error', 4],
        comma-dangle: ['error', 'never']
    }
